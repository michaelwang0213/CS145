Contians answer to homework and python files. 

*hw4.pdf - contians answers to homework questions and graphs of output running each of the three algorithms on each of the three datasets.

All python files retain same behavior as described in assingment readme, ie:

*DataPoints: contains a helper class/functions

*KMeans.py - runs the KMeans algorithm on the given dataset and writes the results to kmeans.csv.

*DBSCAN.py- runs the KMeans algorithm on the given dataset and writes the results to dbscan.csv.

*GMM.py- runs the KMeans algorithm on the given dataset and writes the results to GMM.csv.